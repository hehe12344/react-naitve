import React from 'react';
import {View,Text} from 'react-native';
const person = function(){

return <Text>con nguoi ne!{Math.floor(Math.random()*30)}</Text>
}
export default person;















































